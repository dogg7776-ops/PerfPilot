name: Build PerfPilot v2

on:
  push:
    branches: [ "replica-v2" ]
    paths:
      - "replica-v2/**"
      - ".github/workflows/build-perfpilot-v2.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"
      - name: Build debug APK
        working-directory: replica-v2
        run: gradle :app:assembleDebug --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: PerfPilot-v2-alpha1-debug
          path: replica-v2/app/build/outputs/apk/debug/app-debug.apk
